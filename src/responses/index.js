const ListResponse       = require('./list');
const PaginationResponse = require('./pagination');
const ViewResponse       = require('./view');

module.exports = {
	ListResponse,
	PaginationResponse,
	ViewResponse,
};
